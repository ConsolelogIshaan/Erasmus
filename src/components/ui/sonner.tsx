"use client";

import type { ToasterProps } from "sonner";

/**
 * Toast notifications permanently disabled per user preference.
 */
function Toaster(_props: ToasterProps) {
  return null;
}

export { Toaster };
